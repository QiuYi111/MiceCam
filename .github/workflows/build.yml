name: Build MiceCam

on:
  push:
    branches: [ "main", "feat/*", "fix/*" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-macos:
    runs-on: macos-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install System Dependencies (macOS)
      run: |
        brew install pkg-config nasm autoconf automake libtool

    - name: Setup vcpkg caches
      uses: lukka/run-vcpkg@v11
      with:
        vcpkgGitCommitId: 3c62cf3457a4128f7fb7e60b865525d88f615f21
        vcpkgDirectory: ${{ github.workspace }}/vcpkg
        vcpkgJsonGlob: '**/vcpkg.json'

    - name: Configure CMake
      run: >
        cmake -S ${{ github.workspace }} -B ${{ github.workspace }}/build
        -DCMAKE_BUILD_TYPE=Release
        -DCMAKE_TOOLCHAIN_FILE=${{ github.workspace }}/vcpkg/scripts/buildsystems/vcpkg.cmake

    - name: Build All Targets
      run: cmake --build ${{ github.workspace }}/build --config Release -j$(sysctl -n hw.ncpu)
