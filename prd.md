。这个项目旨在实验室高速采集相机数据。我希望你能用 c++实现，采用独立 backend 架构支持多种相机驱动。数据流方面，采用三阶段架构，第一阶段直接写二进制 raw 流，存储为.bin 文件，保证最大的帧率。这个阶段需要用 json 文件保存元数据，并且存储每一帧的时间戳。第二阶段，将.bin 转换成 包含元数据、时间戳的 hdf5 文件，便于后续处理和分析。第三阶段，允许用户选择时间，将选中的 session 转移到指定的磁盘上，可以是映射到本地的 NAS。 你可以参考的项目是 ~/camera，但那个项目已经过于臃肿和复杂，难以维护了。开发时，请善用 tmux 和 Claude code 帮你完成代码编写，测试等工作。另外，非常重要的是一定要遵循 TDD 开发流程，测试套件包括单测集测，功能测试和压力测试，你可以用 mockdata 进行流水线测试。 mvp 版本需要你实现对 USB webcam 的支持即可。但记住相机驱动 backend 需要是模块化的，可拓展的。

🛠️ 补充约束与技术细节
1. 并发与内存管理：
• 非阻塞架构：采集线程（Ingestion）必须与写入线程（I/O）解耦。请实现一个**环形缓冲区（Ring Buffer）或双缓冲（Double Buffering）**机制，确保第一阶段写盘不会阻塞相机取帧。
• 零拷贝意识：在各阶段数据传递时，尽可能使用 std::unique_ptr 转移所有权或使用内存池，避免大尺寸图像帧的频繁拷贝。
2. 数据完整性与可追溯性：
• 严格时间戳：时间戳必须在从硬件/驱动层获取到 Buffer 的瞬间立即打标，使用 std::chrono::high_resolution_clock。
• 原子写入：第一阶段的 JSON 元数据应包含文件校验码（如 MD5/CRC32），确保第二阶段转换时数据未损坏。
3. TDD 深度要求：
• 压力测试指标：编写一个测试用例，模拟 200MB/s 以上的持续写入，并监控丢帧率（Drop Rate）。
• Mock 注入：提供一个 FakeCamera 类，能按预设的 FPS 生成带有序列号的测试图案，用于闭环测试流水线的准确性。
4. 部署与工具：
• CMake 模块化：采用现代 CMake 编写，Backend 驱动应作为插件或静态库链接，确保核心逻辑不依赖特定相机 SDK。
• 环境自检：提供一个 check_env.sh，用于在 tmux 中启动前检查磁盘写入速度和依赖项。
