[project]
name = "micecam-tools"
version = "0.1.0"
description = "Tools for processing MiceCam data"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "opencv-python",
    "h5py",
    "depthai>=3.3.0",
]

[tool.uv]
managed = true
